/* O que são vetores ou arrays

 Como declarar um array:
    let array = ["string", 1, true];
    console.log(array);

Pode guardar vários tipos de dados: 

let array = ["string", 1, true, ["array1"], ["array2"], ["array3"], ["array4"]];
//console.log(array[5]);

//ForEach

/*array.forEach(function (item, index) {
  console.log(item, index);
}); 

// Push adiciona um novo item (qualquer dado) no final do array.

array.push("novo item");
console.log(array); 


// Pop remove o ultimo item do array. 

array.pop();
console.log(array);

// Shift remove o primeiro item do array;

array.shift();
console.log(array);

// Unshift insere novo item no início do array.

array.unshift("novo item");
console.log(array);

//IndexOf retorna o índice de um valor.
console.log(array.indexOf(true));

// Splice remove ou substitui um item pelo índice
array.splice(0, 3);
console.log(array);

//Slice retorna uma parte do array existente

let novoArrey = array.slice(0, 3);
console.log(novoArrey);


//Manipulando Objetos

let object = {
  string: "string",
  Number: 1,
  Boolean: true,
  array: ["array"],
  objectInterno: { objectInterno: "objeto interno" },
};

 console.log(object.objectInterno);

var string = object.string;
console.log(string);

var arrayInterno = object.array;
console.log(arrayInterno); 

var { string, Boolean, objectInterno } = object;
console.log(string, Boolean, objectInterno);
*/
